{
  "name": "wonderland",
  "version": "1.0.0",
  "private": true,
  "description": "Wonderland is a learning-focused full stack web application for a modern theme park experience.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "start": "concurrently -k -n BACKEND,FRONTEND -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev": "npm start",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "postman:generate": "npm run postman:generate --prefix backend",
    "test:e2e": "npm run test:e2e --prefix frontend",
    "test:e2e:headed": "npm run test:e2e:headed --prefix frontend",
    "test:e2e:ui": "npm run test:e2e:ui --prefix frontend",
    "test:e2e:report": "npm run test:e2e:report --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
